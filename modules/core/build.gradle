version = "$core"

dependencies {
    api(project(":localization"))
}

java {
    withSourcesJar()
    withJavadocJar()
    tasks.withType(Jar).each { it.archiveBaseName.set("mfxcore") }
}

mavenPublishing {
    publishToMavenCentral()
    signAllPublications()
}


